"""agentdiff — git diff, but for AI agent runs.

Align two agent run trajectories step-by-step, mark where they diverge, and
report token / cost / latency deltas. Runs fully local, format-agnostic.
"""

__version__ = "0.1.0"
